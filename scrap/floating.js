/*let floating = document.getElementsByClassName('floating');

window.addEventListener('load', () => {
    for (let i = 0; i < floating.length; i++)
    {
        floating[i].style.marginTop = '0px';
    }
});

window.addEventListener('scroll', () => {
    for (let i = 0; i < floating.length; i++)
    {
        console.log(floating[i].style.marginTop);
        //let currentVal = parseInt(floating[i].style.marginTop.substring(0, floating[i].style.marginTop.length - 2));

        // Move it by the difference in scroll, 
        floating[i].style.marginBottom = window.scrollY / 3;
    }
});
*/